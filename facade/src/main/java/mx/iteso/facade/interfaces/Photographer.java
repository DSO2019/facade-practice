package mx.iteso.facade.interfaces;
/**photographer. */
public interface Photographer {
    /**turn on camera. */
    void turnOnCamera();
    /**turn off camera. */
    void turnOffCamera();
    /**take photo. */
    void takePhoto();
    /**take video. */
    void takeVideo();
}
